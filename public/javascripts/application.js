// Generated by CoffeeScript 1.3.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  jQuery(function() {
    var view;
    window.LocalNightly = {
      Model: {},
      View: {},
      Collection: {},
      Router: {}
    };
    LocalNightly.Model.Event = (function(_super) {

      __extends(Event, _super);

      Event.name = 'Event';

      function Event() {
        return Event.__super__.constructor.apply(this, arguments);
      }

      return Event;

    })(Backbone.Model);
    LocalNightly.Collection.Events = (function(_super) {

      __extends(Events, _super);

      Events.name = 'Events';

      function Events() {
        return Events.__super__.constructor.apply(this, arguments);
      }

      Events.prototype.model = LocalNightly.Model.Event;

      return Events;

    })(Backbone.Collection);
    LocalNightly.View.Master = (function(_super) {

      __extends(Master, _super);

      Master.name = 'Master';

      function Master() {
        return Master.__super__.constructor.apply(this, arguments);
      }

      Master.prototype.el = 'body';

      Master.prototype.initialize = function() {
        $(document).scroll(function() {
          var position;
          position = $(document).scrollTop();
          if (position !== 0) {
            return $('#logo').fadeOut('slow');
          } else {
            return $('#logo').fadeIn('slow');
          }
        });
        return this.render();
      };

      Master.prototype.render = function() {
        var container;
        console.log('rendered');
        return container = new LocalNightly.View.Container;
      };

      return Master;

    })(Backbone.View);
    LocalNightly.View.Container = (function(_super) {

      __extends(Container, _super);

      Container.name = 'Container';

      function Container() {
        return Container.__super__.constructor.apply(this, arguments);
      }

      Container.prototype.el = '#container';

      Container.prototype.initialize = function() {
        var _this = this;
        window.events = new LocalNightly.Collection.Events;
        events.url = '/api/las-vegas';
        return events.fetch({
          success: function() {
            return _this.render();
          },
          error: function() {
            return console.log(':(');
          }
        });
      };

      Container.prototype.render = function() {
        var _this = this;
        return events.each(function(e) {
          console.log(e.get('picture_url'));
          return $(_this.el).append("<img src=" + (e.get('picture_url')) + " />");
        });
      };

      return Container;

    })(Backbone.View);
    /*
    	class LocalNightly.Router.Route extends Backbone.Router
    		routes :
    			"!/": "home"
    			"!/tester": "tester"
    
    		home: ->
    			@homeView = new LocalNightly.View.HomeView
    			$("#content").html @homeView.el
    			console.log(@homeView.el)
    		tester: ->
    			unless @testView
    				@testView = new LocalNightly.View.TestView
    			$("#content").html @testView.el
    			console.log(@testView.el)
    */

    return view = new LocalNightly.View.Master;
  });

}).call(this);
